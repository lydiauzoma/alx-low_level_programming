#include "main.h"
/**
 * print_sign - prints the sign of a number
 * @n:input number
 *
 * Return: 1 if positive,0 if negative
 */
int print_sign(int n)
{
	if (n > 1)
	{
		_putchar('+');
		return 1;
	{
	else (n == 0)
	}
		_putchar('0');
		return 0;
	}
	else
	{
		_putchar('-');
		return -1;
	 } }
