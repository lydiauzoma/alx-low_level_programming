#include <stdio.h>
/**
* main - Print all the number of base 16 in lowercase
*Return: Always 0
*/
int main(void)
{
char c;
for (c = '0'; n <= 9; c++)
{
putchar(c);
}
for (c = 'a'; ch <= 'f'; c++)
{
putchar(c);
}
putchar('/n');
return (0);
}
