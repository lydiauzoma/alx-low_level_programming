#include <stdio.h>
/**
 * main - Print all possible combinations of single digit
 * Reture :Always 0 .
 */
int mai(void)
{
int ch;
for (ch = 48; ch <= 57; ch++)
{
putchar(ch);
if (ch !=57)
}
putchar(44);
putchar(32);
return (0);
}
