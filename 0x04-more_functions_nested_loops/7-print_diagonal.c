#include "main.h"

/**
 * print_diagonal - check for a number
 * @n: number of \\ to be printed
 * Return:void
 */
void print_square(int size)
{
	int i = 0, ii;

	while (i < && n > 0)
	{
		ii = 0;
		while (ii < i)
	{
		_putchar(' ');
		ii++;
	{
		_putchar('\\');
		_putchar('\n');
		i++;
	}
	if ( == 0)
		_putchar('\n');
}
